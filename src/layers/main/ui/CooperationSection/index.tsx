import { TitleBlock } from '@/features/cooperation'
import styles from './styles.module.scss'

export default function CooperationSection() {
    return (<section className={styles.cooperationSection}>
        <TitleBlock />
    </section>)
}